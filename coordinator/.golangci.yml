version: "2"

run:
  timeout: 3m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  max-same-issues: 50

exclusions:
  rules:
    - path: "_test\\.go"
      linters:
        - errcheck
