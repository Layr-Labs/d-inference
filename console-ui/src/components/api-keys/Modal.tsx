// Re-export of the shared dialog shell. Kept so existing `./Modal` imports in
// this folder keep working; the implementation now lives in components/ui.
export { Modal } from "@/components/ui/Modal";
