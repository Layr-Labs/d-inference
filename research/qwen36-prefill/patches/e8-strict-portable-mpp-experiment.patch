diff --git a/mlx/backend/metal/device.cpp b/mlx/backend/metal/device.cpp
--- a/mlx/backend/metal/device.cpp
+++ b/mlx/backend/metal/device.cpp
@@ -966,6 +966,12 @@ bool is_nax_available() {
 #ifdef MLX_METAL_NO_NAX
   return false;
 #else
+  // EXPERIMENT ONLY: measure the strict-precision MPP shader fallback on M3.
+  // Defaults retain the shipping Apple10/M5-era generation gate.
+  if (env::get_var("MLX_FORCE_NAX_EXPERIMENT", "") == "1") {
+    return __builtin_available(
+        macOS 26.2, iOS 26.2, tvOS 26.2, visionOS 26.2, *);
+  }
   auto _check_nax = []() {
     bool can_use_nax = false;
     if (__builtin_available(
diff --git a/mlx/backend/metal/kernels/steel/gemm/nax.h b/mlx/backend/metal/kernels/steel/gemm/nax.h
--- a/mlx/backend/metal/kernels/steel/gemm/nax.h
+++ b/mlx/backend/metal/kernels/steel/gemm/nax.h
@@ -404,7 +404,7 @@ struct NAXFrag {
         16,
         transpose_a,
         transpose_b,
-        true,
+        false,
         mpp::tensor_ops::matmul2d_descriptor::mode::multiply_accumulate);
 
     // Create matmul op
@@ -476,7 +476,7 @@ struct NAXFrag {
         16,
         transpose_a,
         transpose_b,
-        true,
+        false,
         mpp::tensor_ops::matmul2d_descriptor::mode::multiply_accumulate);
 
     // Create matmul op
