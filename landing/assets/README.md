# Inference test fixture

`cube-hero.png` is retained at its original path because connected-cache
vision fixtures reference these exact bytes and a pinned hash. It is not
part of the landing page UI. See
[the connected-cache test instructions](../../docs/developer/test.md).
