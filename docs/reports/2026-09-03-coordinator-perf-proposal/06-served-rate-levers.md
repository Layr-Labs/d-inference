# 06 — Served-rate levers: what is still open on master `5d400cf75`, what each is worth, and in what order

Date: 2026-09-03. Read-only analysis; no code, no prod mutation. Code citations are to master `5d400cf75`
(`coordinator/` paths) unless prefixed by a branch. Grades: **M** = measured by the cited research/PR/snapshot,
**C** = derived by arithmetic on measured inputs (shown), **E** = estimate/assumption (mine, stated).
Research = `docs/research/2026-09-02-utilization-research/` on branch `worktree-bridge-cse_01KVHTuWPYyR4QSzGNe47pR7`
(cited as `RR §` for README, `01 §`, `07 §`, `08 §`, `09 §` for topic reports; only 00–09 exist on the branch —
the README's "report 12" was never committed). Opt-pass = `docs/reports/2026-09-02-provider-system-optimization.md`
on branch `worktree-bridge-cse_01SAkmgZHFsFMVXijYt6Czjp` (commits `773bee1b3`, `58d7792da`; no PR).

## Question

Which levers that raise served/offered are still open on master (vs merged, vs parked in a branch/PR), what
served-rate delta does each claim from the research's own numbers, in what order should they ship, and what
does today's snapshot (66 scan-semaphore waiters, ~150 `Registry.mu` waiters, attempt tails to 11+) add?

## Baseline (cited, not re-derived)

| Quantity (last 24 h, client level) | Value | Grade / source |
|---|---:|---|
| Offered / served | ≈3.96M / ≈3.13M (**79 %**) | M, RR §1 |
| Preflight `routing_saturated` (could_have_served=true) | ≈579K/day (14.6 %) | M, RR §1, 07 §3.5 |
| Dispatch `first_chunk_timeout` | ≈156K/day (4.0 %); ≈20–25 % terminate on <0.8.15 boxes (12–14 % firm) | M, RR §1, 06 §1.4 |
| Dispatch `deadline_unreachable` / dispatch `routing_saturated` | ≈24K / ≈23K per day | M, RR §1 |
| Doomed chains | gpt-oss ≈112K/day (16 % of entered, 11.1 attempts), gemma ≈120K/day (5 %, 3.6) | C, 07 §3.8 |
| gpt-oss attempts/request; attempt ≥4 success | 3.2; 2.0 % (88 % `deadline_unreachable`) | M, 07 §3.3 |
| Lock state (2026-09-02 dumps) | 142 goroutines on `Registry.mu.Lock` (92 commit), 39 on the 96-permit semaphore, 1 scanning | M, 07 §3.2, 08 §1.1 |
| Route delay → outcome | gemma a0 success 96 % (route 1–3 s) → 88 % (3–10 s) → 7 % (>10 s); gpt-oss 56 → 30 → 0.4 % | M, 07 §3.6 |
| Today 14:07 PDT: `inference_routes`/h by attempt | 0→135,754; 1→16,941; 2→9,034; 3→3,887; 4→2,751; 5→1,829; 6→1,395; 7→1,094; 8→888; 9→696; 10→588; 11→526 (+tail) | M, prod snapshot |
| Today: `request_rejections` +40,411/h; 46 chat/s; 66 in `acquireRoutingScanSlot` (server.go:1392); ~150 on `Registry.mu` (88 at `commitProviderReservation` scheduler.go:528, rest in outcome recorders) | — | M, prod snapshot |
| Today, derived: rejections ÷ (attempt-0 + rejections) | 40,411 ÷ 176,165 ≈ **23 %** (upper bound: mixes preflight and dispatch stages) — same regime as the research's 21 % | C |
| Today, derived: retry rows/h (attempts 1–11) | 39,629 ≈ 0.29 retries per entered request, ≈11/s; attempt ≥7 ≈ 3.8K/h ≈ 1/s | C |

Prod knobs that matter here (verified 2026-09-03 unless noted): `QUEUE_BEFORE_SHED=true`, `QUEUE_MAX_DEPTH=8`,
`QUEUE_MAX_WAIT=6s`, `TTFT_ADMISSION_MODE=shadow`, `TTFT_HARD_REJECT=false`, `TTFT_LIVE_DEADLINE_BASE_MS=9000`,
`CACHE_ROUTING_MODE=off`, `MIN_PROVIDER_VERSION=0.7.5` (06 §3.0 + `deploy/environments/prod.env:36`),
`ROUTING_CONCURRENCY=96` (07 §3.1, 2026-09-02; not in today's list), `COLD_DISPATCH=false` (06 §3.0).
Discrepancy: `deploy/environments/prod.env:99` says `TTFT_HARD_REJECT=true`; the live container says `false` — the
repo copy is stale (docs hygiene item, no served-rate effect).

## Lever table

Status legend: **merged** = on `5d400cf75`; **branch** = built but unmerged (no PR); **PR #n** = open PR; **env** =
knob exists, prod value is the lever; **not built**. Effort is mine (E), in eng-days, including tests and rollout.

| # | Lever | Mechanism · master file:line | Status | Claimed served-rate delta (grade, source) | Effort | Risk | Depends on |
|---|---|---|---|---|---|---|---|
| a1 | Preflight `routing_saturated` shed + scan semaphore (#799) | `routingScanSem` cap = `NumCPU` default, `EIGENINFERENCE_ROUTING_CONCURRENCY` override (server.go:503-516, 1339-1356; prod 96). Preflight waits `deadline/4` capped **1 s** (inference_admission.go:227-236, constants — there is **no** `EIGENINFERENCE_ROUTING_SCAN_*` knob) and sheds at :308-340 **before any walk**, so `QUEUE_BEFORE_SHED` (cold_dispatch.go:20-24, only the `machine_busy` path) can never catch it. Dispatch attempts park for the **whole remaining budget** (consumer.go:1091-1114; dispatch.go:1287-1298 latches one 429). | merged | Re-scoping alone: **≤ 0** — ungating the preflight before the lock fix hands each shed request a permit for its whole budget and moves the failure to `first_chunk_timeout` (M/C, 08 §4; RR "do not" list). The lever is a2, then re-scope (4c). | 1 (4c, after a2) | low once ordered | a2, step-0 metric |
| a2 | `Registry.mu` write-lock fix (P1 recorders + P2 commit under `RLock`+`p.mu`, one unit) | Success path = 6 write acquisitions: commit scheduler.go:528; first-content `RecordCapacityAccept` → capacity_cooldown.go:421; error_cooldown.go:149; provider_breaker.go:193; health_ejection.go:456; registry.go:2501. Writers park behind every in-flight walk (07 §4.2 bench: 22–35 writers/s once readers present). | **not built** (no PR; not on the opt-pass branch either) | **+537K/day** counterfactual ceiling (C, 07 §6); adversarial range **+250K–550K/day, point ≈ +400K** (C, 08 §4). Sheds → <5 % of today, route p50 1.7 s → 30–80 ms, ≈75 % likely (08 §5). | 8–10 (P1+P2 as one release behind `EIGENINFERENCE_RESERVE_COMMIT_MODE`, `-race` double-spend test, shard maps by fault key, failure-path recorders too) | medium (double-spend gap closed only by the global lock today) | step 0: `registry.mu` wait histogram by call site (08 §6.1) |
| a3 | Walk-CPU cuts (P3): memoized medians, cached version parse/fault keys, `time.Now` once, skip `prefixCacheV2CapabilitiesForModel` walk when cache routing is off (scheduler.go:572 runs it unconditionally) | ~65–70 % of scan CPU removable (C, 07 §3.7); shorter walks = shorter writer stalls | **branch** (opt-pass wave 2 `58d7792da`: `tps_registry.go`/`solo_tps.go` memoization, `model_index.go`) | No standalone served claim; raises a2's ceiling ("useful, not sufficient", 08 §5) | 2–3 (rebase onto #799/#809) | low | ships with a2 |
| b | TTFT admission live / hard reject | `EIGENINFERENCE_TTFT_HARD_REJECT` (main.go:466), `TTFT_ADMISSION_MODE` (main.go:538); `enforce` ≡ `shadow`, documented no-op (ttft_shadow.go:44-51). Margin/gating from 01 P3 not built. | merged machinery, **not live**; margin **not built** | **≤ 0 today**: 1.0× ceiling wrongly excludes 7–14 % of ≥8K candidates that then succeed (3–6 % at 2.0×); `best_ttft` exceeded budget in 0.0 % of rows so the preflight 429 would ~never fire — value is latency/load, not served (M, 01 §3.8) | 3 (margin + `observedPrefillTPS` gate + shadow `would_exclude_all` counter) | medium before d | d (honest projection) |
| c | Injected `max_tokens` in the routing cost term | Still injected: `ensureMaxTokensBound` consumer.go:1725-1753, bound = registry `max_output_length` (consumer.go:1935-1972; 32,768 for gpt-oss **and** gemma, 09 §2; fallback 8,192 consumer.go:1502). Feeds `thisReqMs = prompt/prefillTPS + reqMax/effectiveTPS` (scheduler.go:2083-2085, **2144**) and `waitingBacklogTokens` (:2120) against a 3 s near-tie window (:39) — gpt-oss `this_req_ms` p50 ≈400 s (M, 07 §3.8). | 5a rank-on-expected: **branch** (opt-pass `773bee1b3`: `api/routing_estimates.go`, `registry/completion_calibration.go`, replay gini 0.72 → 0.00); 5b per-model cap 8,192: **not built** | No direct served number in the research. Mechanism claims: top-10 share 34 % → ≈13 % (C, 09 §3 / RR item 3), attempt ≥2 rescans −⅓ to −½ (E per 07 §6); injected-bucket refusal 30.1 % → ≈9.3 % at the ≤1K occupancy mix (C, 09 §3). **My E: +15–40K/day** — assumes herding rescues only the short/mid-prompt share of chain deaths (≤37 % of gpt-oss's 82.7K deadline-attributable loss, 02 §1.7, plus gemma's non-physics share); the ≥8K tail needs d. | 3–4 (rebase 5a + shadow log; 5b = env CSV + clamp) | low–medium (5b is product-visible: `finish_reason=length` 0.63 % at 8,192, 09 §4; never 4,096) | 5b after 5a; keep admission/billing on the bound |
| d | Provider deadline projection haircut + un-wedge (cite only) | Window ratio ≈0.5× marginal, × `deadlineProjectionRateHaircut = 0.5` (`EngineV2Bridge.swift:345`) → projection ≈¼ capability; refusals record no sample (02 §1.2-1.3) | **not built** for v0.8.16 (#791 activation floors merged, projection untouched); refusal-driven re-probe exists on the opt-pass **branch** (provider commit `eb2f84bb3`) | gpt-oss client success 72 % → 79–81 % of offered (**+55–70K/day**), gemma 80.7 % → ≤83 % (≤ +68K/day) → **+60–130K/day** (C, 02 §1.7); removes ≈1.2–1.4M refusal attempts/day | provider release train (weeks) | medium: optimism moves 429s to uptime-counting 504s; gate on `admitted_but_failed` ≈ 0 | — |
| e | Retry ladder bound / refusal chains | `maxDispatchAttempts = 64` (consumer.go:84), `maxFirstChunkTimeoutRetries = 3` (:110, #799), `maxCapacityClassRetries = 3` (:94); **no** cap on the `deadline_unreachable` class (dispatch.go:197). Tail today: attempt ≥7 ≈ 3.8K rows/h ≈ 1 scan + 1 commit write per second. | class caps **merged**; `maxDeadlineRefusalRetries` **not built** (#757 draft touches retry ordering but is pre-#792/#793/#799 and stale since 08-27) | K=6 is **served-negative**: −0.05–0.08 % of <2K successes, −2–14 % of long-prompt successes-after-refusal, while saving 41–64 % of long-prompt refusal scans (M, 01 §3.12). Load saving today ≈1 scan/s (C, snapshot) — trivial. | 1 | medium before d (converts recoveries into 429s) | d, then c |
| f | First-content clock (#787's 4 s cutoff) | Global: `defaultFirstContentDeadlineBase = 5 s` (consumer.go:56) overridden to **9 s + 1 ms/token** by `TTFT_LIVE_DEADLINE_BASE_MS=9000`; upstream 10 s + 1 s headroom (modelpolicy/first_content_deadline.go:10-30). Qwen3-VL exact pair **5 s / 4 s still hardcoded** (:22-30, :60-67); #787 **not reverted**; #799 added `EIGENINFERENCE_MODEL_FIRST_CONTENT_BASES` (main.go:683) — **unset** in today's env, so the 4 s cutoff is live for qwen3-vl. | merged; override **env, unset** | ≈ 0 today: qwen3-vl is not listed on OpenRouter (05 §5.1); the 2026-08-31 cascade shows the cost if listed under 4 s (RR item 14). Root cause of qwen3-vl TTFT = generation time is the provider think-parser `.undecided` buffering (opt-pass §2.2, provider-side, branch). | 0 (env: `qwen3-vl-30b-a3b-instruct=off` or `8000`) | none | set before any qwen3-vl listing |
| g | Hedging / speculative dispatch at 50 % | `speculativeTimerRatio = 0.5` (consumer.go:112-116, const, no env); governor caps hedges at idle/4, 2-slot headroom, win-rate floor 0.10 (hedge_governor.go:28-90); +2 fleet walks per request (07 §4.1). | merged (#792) | Cost: 110,301 attempt-0 rows/day carry the backup flag ≈ 55K hedged requests/day, ≈1.4 % (M, RR §7). Benefit (win rate) **not measured** by the research — no served claim either way. | 0 | — | leave; after a2 fold the idle-alternative walk into the plan scan (07 P3.6) |
| h | Warm pool sizing / cold routing (645 cold boxes) | config.go:121-144 knobs; prod `MIN_WARM gpt-oss=16, gemma=32`, `COLD_DISPATCH=false`; cold dispatch = 0.07 % of attempts | merged; #807 (open, proactive headroom — no served claim in its body), #808 (draft, diagnostics), #756 (draft, docs only), autopilot design (branch) | **≈ 0**: Little's-law need ≈45 warm boxes vs 1,004 warm slots — the cold half is surplus, not starvation (M/C, 06 §1.1); long-prompt placement value overlaps d | — | — | not a served-rate lever; separate program |
| i | Wedge detection / eviction | Heartbeat `WedgeSuspected` is telemetry only (provider_wedge_telemetry.go:86-93); carried in the routing snapshot (scheduler.go:189, 1795) but **consulted by no gate**; `deadline_unreachable` is health-neutral (consumer.go:352-355, dispatch.go:1739). One session took 28.6 % of gpt-oss first dispatches over 3 h and served 0 (M, RR §0). | narrow skip **not built**; provider-side un-wedge **branch** (opt-pass) | gpt-oss attempt-0 refusal 48 % → ≈27 %, top-10 share 34 % → ≈13 % (C, 09 §4-5 / RR item 3); 7–14K wasted attempts/h per wedged slot → ≤6 probes/h. Served delta not quantified by the research; **my E: +5–20K/day** (short-prompt chain deaths whose budget the wasted attempt-0 consumed; 44 % of gpt-oss short successes already pay +505 ms, 01 §6). Human action today (evict/restart `45e6032f…`) captures most of it for free. | 2 (skip keyed on refusals of <512-token prompts on **empty** slots, N=5, TTL ≤120 s, half-open, clear on first content) | low for the narrow form; the broad derater fires on 93 % of cells (M, 09 §6) | — |
| j | Queue-drain full-fleet scan per queued request per heartbeat | `drainQueuedRequestsForModelsWithReason` scheduler.go:3087-3180 runs `ReserveProviderEx` per queued request with no early exit; called from every heartbeat (registry.go:4096), disconnect (:5010), challenge (:5332), idle (:5448) — on the provider read loop, outside #799's semaphore. **Still O(fleet × queue) on master.** | fix **branch** (opt-pass `queue_drain_dominance.go`, `queue_drain_suppress.go`; bench 48 ms → 1.4 ms per heartbeat pass) | **≈ 0 today**: queue effectively unused (0–7 queued rows/h; 63 of 2.46M gemma a0 rows had `queue_wait_ms > 0`; depth 8) (M, 07 §3.4, 01 §3.6). Becomes load-bearing once a2/4c admit the shed and queue-before-shed starts queueing. | 1–2 (rebase) | low | ship before 4c |
| k | Minimum provider version 0.7.5 → 0.8.15 | Register-time reject at provider.go:452; env `EIGENINFERENCE_MIN_PROVIDER_VERSION` (server.go:364). <0.8.15 = 4.3 % of fleet, 6.5 % of attempts, 1.6 % of served; 4–5.5 refusals per served request vs 0.59 on 0.8.15 (M, 06 §1.4). | **env** (prod 0.7.5) | 20–25 % of client-visible FCT terminate on them (upper causal bound), 12–14 % firm → 156K × 0.12–0.25 = **+19–39K/day** (C, RR item 1); −241K refusal attempts/day; ≈50K displaced requests/day ASSUMED absorbable at 5 % utilization | 0.5 (stage 0.8.12 → 0.8.15; #778 already merged so upgrade waves no longer strike trust) | low | — |

## Shipping order + combined estimate

Order maximizes served requests/day per eng-day; one behaviour change per 24 h so effects stay attributable
(RR §4). Overlap rule: `routing_saturated` recovery is counted **net** of the requests that, once admitted, die as
`first_chunk_timeout` in the same windows (08 §4 already nets this: entered requests serve 91–94 % in shed
windows, hence the 08 range rather than 07's ceiling). Version-floor and wedge gains act on the FCT/chain classes
and are counted once.

| Tranche | Ships | Eng-days | Served delta/day | Served fraction after | Assumptions |
|---|---|---:|---:|---:|---|
| 0 (day 0–1, ops/env) | k (0.8.12 then 0.8.15); i-human (evict the wedged session); f-env (qwen3-vl `off`/8000); fix stale `prod.env:99` | 0.5 | +25–55K (k +19–39K C; wedge +5–20K E) | 79 % → **≈80 %** (+0.6–1.4 pt) | displaced 50K/day absorbable (ASSUMED, RR item 1) |
| 1 (days 2–5) | step-0 `registry.mu` wait histogram by call site; **N1** below (first-byte recorder off the write lock); i narrow wedge skip (durable form of the human action) | 3–4 | 0 counted (N1 unquantified; i already counted in T0) | ≈80 % | N1 is latency + 504-avoidance; measure before claiming |
| 2 (weeks 2–3) | a2 (P1+P2 one unit) + a3 (rebase opt-pass wave 2) + j (rebase) → then 4c (semaphore never gates preflight; shed on measured wait; shed retries before fresh) | 10–13 | **+250–550K, point +400K** (C, 08 §4) | **≈86–93 %, point ≈90 %** | shed requests serve at calm-bucket rates; no OpenRouter double-send inside 2 min (RR §5 checked); 4c only after wait p99 < 50 ms for days (08 §4) |
| 3 (week 4) | c: 5a rank-on-expected (rebase `773bee1b3`, shadow-logged then live) → 5b cap 8,192 gpt-oss+gemma | 3–4 | +15–40K (E) | ≈**91 %** | herding-caused chain deaths are the short/mid-prompt share; long-prompt occupancy brake shipped with 5a (09 §5) |
| 4 (provider v0.8.16, weeks; cite only) | d honest projection + un-wedge; then e (K=6) and b (2.0× margin, shadow first) as load backstops | provider train | +60–130K (C, 02 §1.7); e/b ≈ 0 or slightly negative | ≈**93 %** | 429s do not become 504s (`admitted_but_failed` stays ≈0) |
| residual | physics tail (>36K prompts on <1,000 tok/s chips), `client_gone`, provider errors, unclassified ≈1 % | — | — | ≈7 % unserved | RR §1 residual row |

Per eng-day: T0 ≈ 50–110K/day per eng-day; T2 ≈ 25–45K/day per eng-day but 10× the absolute gain; T3 ≈ 5–10K/day
per eng-day. T0 first, then T2 — nothing else moves the 14.6 % class.

## New levers from today's snapshot

| # | Finding (grade) | Lever |
|---|---|---|
| N1 | **A write-lock acquisition sits between provider first content and client first byte.** `commitFirstContent` (dispatch.go:594-626) runs on the dispatch goroutine when the first content chunk is read, calls `RecordCapacityAccept` → `RecordCapacityAcceptOutcome` (capacity_cooldown.go:397-425), which takes `r.mu.Lock()` **unconditionally** when `capacityRateCfg.PenaltyMs > 0` (default 15,000, capacity_rate.go:58) — before `d.committed = true` (dispatch.go:2062-2063) and before `writeCommittedResponse` (dispatch.go:3663). Mean write wait ≈0.4 s, commit sub-queue 1.8–2.8 s at peak (C, 07 §4.2); today ~60 of ~150 lock waiters are outcome recorders (M, snapshot). A request whose provider already produced content can cross OpenRouter's 10 s + 1 ms/token line inside this wait and become a **504 (uptime-counting)**, not a 429. | Record the accept asynchronously (or after the first client write): ~20 LOC, no semantic change (bookkeeping only; the completion-time re-offer path already exists, capacity_cooldown.go:405-418). Ship in T1 with the wait histogram; the histogram's `RecordCapacityAcceptOutcome` call-site bucket is the served/504 number. Grade E until measured. |
| N2 | **The semaphore wait is TTFT loss, measured today.** 66 waiters at ≈95 permit acquisitions/s (46 preflight + ≈49 dispatch attempts/s) → mean permit wait ≈0.7 s by Little's law (C; matches 07's 0.77 s mean hold), and a dispatch-stage waiter holds for its whole remaining budget (consumer.go:1093). Every second is 1:1 out of the 9 s budget (07 §3.6). | Not a new fix — it is a2 — but it is the evidence that a2 is still open on `5d400cf75` even with #799/#791/#809 merged: same shape as the 2026-09-02 dumps (92 commit + 39 semaphore then; 88 + 66 now). |
| N3 | **Recorders are ≈40 % of the parked writers** (≈60 of ~150 today; 45 of 142 on 2026-09-02, 08 §1.1). | Confirms P1 is real load, but 08 §1.4 shows the ceiling is set by reader concurrency, so P1 ships only as one unit with P2 — do not ship P1 alone expecting the shed to fall. |
| N4 | **Retries are not the lock's load.** ≈11 retry attempts/s × (1 scan + 1 commit write) ≈ 11 writes/s vs ≈226/s from the success path (6 × 37.7 successes/s) ≈ 5 % (C; 07 §3.8 says 7 %). Attempt ≥7 ≈ 1 row/s. | A deadline-refusal cap (e) buys ≈1 scan/s today and costs successes — keep it in T4 as a backstop only. |
| N5 | **Nothing merged since 2026-09-02 moved the served fraction.** Rejections ÷ (attempt-0 + rejections) ≈ 23 % today vs 21 % in the research window (C, upper bound because it mixes stages); 46 chat/s ≈ 4.0M/day offered, same volume. | The proposal's baseline (79 %) still holds for `5d400cf75`. |
| N6 | `prod.env:99` (`TTFT_HARD_REJECT=true`) disagrees with the live container (`false`; 06 §3.0 and today's check). | Fix the sanitized copy; the research read the live env, so no number changes. |

## Not proposed (deliberately)

| Item | Why not | Source |
|---|---|---|
| Cache-routing flip (`CACHE_ROUTING_MODE`), Go-only shadow mode, cache-read price | Separate program; direct served upside is a subset of d's population | 03 §5-6, RR §6 |
| Raising `ROUTING_CONCURRENCY` or adding vCPUs | Host 46 % idle; moves `routing_saturated` into `deadline_unreachable`/FCT | 07 §4.2, RR "do not" |
| Ungating the preflight (4c) before a2, or P1 without P2 | Net-negative: parks shed requests on the writer queue for their whole budget | 08 §4, §1.4 |
| Refusal-rate derater / N-consecutive skip on all prompts | Active on 93 % of gpt-oss cells; trips 44–69 % of serving providers in 3 h | 09 §6 |
| TTFT hard reject / fail-fast live (b) or `maxDeadlineRefusalRetries` (e) before d | Converts servable requests into faster pessimistic 429s; K=6 costs 2–14 % of long-prompt recoveries | 01 §5 ordering, §3.12 |
| Injected cap at 4,096 | Empties 1.1–1.8 % of gpt-oss answers (reasoning ≥ cap); does not un-herd | 09 §4 |
| Hedging changes (ratio, governor) | Governor already bounds it; win rate unmeasured; not a served-rate lever | hedge_governor.go, RR §7 |
| Warm-pool sizing, #807/#808, model autopilot (#756/design) as served-rate levers | Cold half is surplus (≈45 warm boxes needed vs 1,004 warm slots); placement value overlaps d | 06 §1.1 |
| #757 "fleet routing retry/capacity fit" | Stale draft (08-27), pre-#792/#793/#799 and pre-research; supersede by a2 + c rather than rebase | `gh pr view 757` |
| Provider-side changes beyond citation: haircut/un-wedge (d), think-parser streaming, nhooyr 256 KiB frames, drain status | Provider release train; this section is coordinator-scoped | 02 P2, opt-pass §2, §3.2 |
| Listing qwen3-vl, price cuts, 262K context, new $0.05 clones | Tier 4 / "do not" items with no served-rate claim inside this program | RR §3 Tier 4, "do not" |
