# Provider status review screenshot

The screenshot renders the actual `CardRoutingVerdict` and `CardEarningsRow` components from this branch, using labeled synthetic fixtures for readiness, partial model restriction with active requests, and expired status. It is not a production dashboard capture.

Captured with Playwright against a temporary local Next.js preview route. The preview route was removed after capture. The existing application CSP blocked the Vercel development analytics script; the provider components rendered and their details disclosure worked.
