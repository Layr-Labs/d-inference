// Package cacheattempt owns one request's cache preparation and queued-frame
// lifetime. It retains immutable receipt metadata, generation revocation and
// dispatch participation. Registry retains live provider validation;
// cachedirectory owns receipt proofs, holder indexes and their lifecycle.
package cacheattempt
