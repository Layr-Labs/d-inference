// Package providerversion owns provider-version interpretation and its bounded
// lookup caches. Capability checks preserve dotted-segment tolerances, while
// slot-budget layouts intentionally ignore prerelease and build suffixes.
package providerversion
